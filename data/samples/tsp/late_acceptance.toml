# TSP × LateAcceptanceHillClimbing (2-opt).

num_runs = 1

[[instances]]
path = "data/instances/tsp/sample.tsp"
problem = "Tsp"

[[heuristics]]
kind = "LateAcceptanceHillClimbing"
neighbor = "TwoOpt"
# Length of the score history buffer.
history_length = 100

[heuristics.stop_condition]
max_iteration = 100_000
max_duration_secs = 5.0
# max_failed_update = 20_000
