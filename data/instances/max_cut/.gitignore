G*
# Reproduced byte for byte by the generator examples:
#   cargo run --release --example generate_dense_maxcut
generated/
#   cargo run --release --example generate_sparse_maxcut
generated_sparse/
#   cargo run --release --example generate_hard_maxcut
#   (manifest.toml goes with them — it is derived from the same seeds)
hard/
